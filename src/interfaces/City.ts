interface City {
  id: string;
  queryCost: number;
  latitude: string;
  longitude: string;
  resolvedAddress: string;
  address: string;
  timezone: string;
  tzoffset: string;
}
export default City;
